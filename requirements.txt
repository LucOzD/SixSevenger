# Only needed to regenerate Worker data files via the scripts in tools/:
#   tools/gen_lexicon.py           -> worker/src/vader-lexicon.js
#   tools/gen_sentiment_expected.py -> sentiment test fixture
# The application itself is JavaScript and needs no Python at runtime.
nltk==3.10.0
