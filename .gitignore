node_modules/
database.db
recommender_state.pkl
__pycache__/
*.pyc
.env

# Editor settings — local to each machine
.vscode/

# Wrangler's local state and simulated D1 database
.wrangler/

# Documentation. Not needed to run, build or deploy the app.
# worker/wrangler.toml is deliberately NOT ignored: wrangler reads it to deploy
# the Worker (project name, entry point, compatibility date, D1 binding).
*.md
